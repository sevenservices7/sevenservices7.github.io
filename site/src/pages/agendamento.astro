---
import Base from '../layouts/Base.astro';
import Booking from '../components/pages/Booking.astro';
import { useTranslations } from '../i18n/ui';
const t = useTranslations('pt');
---
<Base title={`${t('nav.booking')} — ${t('site.name')}`} description={t('home.cta.body')} routeKey="booking">
  <Booking lang="pt" />
</Base>
